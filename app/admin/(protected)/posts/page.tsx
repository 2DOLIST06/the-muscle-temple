'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { adminApi } from '@/lib/admin/api-client';
interface PostRow {id:string;title:string;slug:string;status:string;isActive:boolean;isIndexable:boolean;robots:string;categorySlug:string;updatedAt:string}
export default function AdminPostsListPage(){const [rows,setRows]=useState<PostRow[]>([]);useEffect(()=>{void adminApi.get<{data?:PostRow[]}>('/admin-api/posts').then((r)=>setRows(r.data??[])).catch(()=>setRows([]));},[]);return <section><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Articles</h1><Link href="/admin/posts/new">Nouveau</Link></div><div className="mt-6 overflow-auto"><table className="w-full text-sm"><thead><tr><th>title</th><th>slug</th><th>status</th><th>isActive</th><th>isIndexable</th><th>robots</th><th>category</th><th>updatedAt</th><th>actions</th></tr></thead><tbody>{rows.map((p)=><tr key={p.id}><td>{p.title}</td><td>{p.slug}</td><td>{p.status}</td><td>{String(p.isActive)}</td><td>{String(p.isIndexable)}</td><td>{p.robots}</td><td>{p.categorySlug}</td><td>{p.updatedAt}</td><td><Link href={`/admin/posts/${p.id}`}>Éditer</Link></td></tr>)}</tbody></table></div></section>;}